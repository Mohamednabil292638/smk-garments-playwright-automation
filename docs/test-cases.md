# SMK Garments Test Cases Specification Matrix

| Test ID | Scenario | Precondition | Steps | Expected Result | Test Type | Automation Status |
|---|---|---|---|---|---|---|
| **TC-NAV-001** | Verify Homepage Loading & Navigation Links | AUT server running | 1. Navigate to `/index.html`<br>2. Check hero title<br>3. Click "Men's Wear"<br>4. Click "Ladies Wear"<br>5. Click "Wholesale"<br>6. Click "Contact" | Page loads successfully, correct URL navigation and page headers displayed | Functional | Automated (`home.spec.ts`) |
| **TC-NAV-002** | Verify Homepage Category Preview Grids | Homepage loaded | 1. Scroll to Men's preview grid<br>2. Count displayed items<br>3. Scroll to Ladies preview grid<br>4. Count displayed items | Preview grids render max 3 items per category with image, title, size tags, and combo pricing | UI / Functional | Automated (`home.spec.ts`) |
| **TC-CAT-001** | Verify Men's Wear Catalog Display | AUT server running | 1. Navigate to `/mens-wear.html`<br>2. Verify total product cards<br>3. Verify badge tags ("Best Seller", "Premium", etc.) | All 5 men's products render with correct title, size tags, unit price, and combo price | Functional / Regression | Automated (`catalog.spec.ts`) |
| **TC-CAT-002** | Verify Ladies Wear Catalog Display | AUT server running | 1. Navigate to `/ladies-wear.html`<br>2. Verify total product cards<br>3. Verify size tags | All 5 ladies products render with correct details and size tags | Functional / Regression | Automated (`catalog.spec.ts`) |
| **TC-PDP-001** | Verify PDP Loading & Breadcrumbs | AUT server running | 1. Navigate to `/product-details.html?id=mens-001`<br>2. Check breadcrumb links<br>3. Check title | PDP loads product "Drop Shoulder Printed T-Shirt", breadcrumbs match category | Functional | Automated (`pdp.spec.ts`) |
| **TC-PDP-002** | PDP Mandatory Option Validation (Negative) | PDP loaded | 1. Do not select size or color<br>2. Click "Add to Cart" | Inline validation messages "Please select a size" and "Please select a color" become visible | Negative | Automated (`pdp.spec.ts`) |
| **TC-PDP-003** | PDP Quantity Controls | PDP loaded | 1. Select size "L" & color "Navy Blue"<br>2. Click "+" button<br>3. Click " Add to Cart" | Quantity increases to 2, total piece count label updates (`= 10 pieces total`), button price updates | Functional | Automated (`pdp.spec.ts`) |
| **TC-MOD-001** | Global Product Selection Modal Trigger | Catalog page loaded | 1. Click "Add to Cart" on catalog card | Modal `#productSelectModal` opens displaying image, title, price, size picker, and color picker | Functional | Automated (`catalog.spec.ts`) |
| **TC-MOD-002** | Global Modal Validation (Negative) | Modal open | 1. Click "Add to Cart" inside modal without selecting options | Size and color error messages display inside modal | Negative | Automated (`catalog.spec.ts`) |
| **TC-CRT-001** | Cart Drawer Open/Close & Count Badge | Product added to cart | 1. Verify header badge count<br>2. Click cart icon<br>3. Click close button | Cart drawer slides open showing added item, count badge displays item quantity, drawer closes | Functional | Automated (`cart.spec.ts`) |
| **TC-CRT-002** | Cart Quantity Increment & Decrement | Item in cart drawer | 1. Open cart drawer<br>2. Click "+" on item<br>3. Click "−" on item | Quantity increases/decreases, subtotal and cart total update automatically | Functional | Automated (`cart.spec.ts`) |
| **TC-CRT-003** | Cart Item Removal | Item in cart drawer | 1. Open cart drawer<br>2. Click trash remove icon | Item is removed from cart, empty cart container is displayed if count reaches 0 | Functional | Automated (`cart.spec.ts`) |
| **TC-CRT-004** | Cart LocalStorage State Persistence | Items in cart | 1. Add item to cart<br>2. Reload page (`page.reload()`)<br>3. Check cart count badge | Cart items persist in `localStorage` across page reloads | Regression | Automated (`cart.spec.ts`) |
| **TC-CHK-001** | End-to-End Purchase & Order Success Flow | Items in cart | 1. Open cart drawer<br>2. Click "Proceed to Checkout"<br>3. Select State "Tamil Nadu"<br>4. Select City "Tiruppur"<br>5. Fill valid customer details<br>6. Submit checkout form<br>7. Click "Complete Order" in prep modal | Checkout validates, Payment Prep opens, Order Success Modal opens with Order ID starting with `SMK...`, item summary, address, and valid WhatsApp link | E2E / Positive | Automated (`checkout.spec.ts`) |
| **TC-CHK-002** | Dynamic State-to-City Dropdown Cascading | Checkout modal open | 1. Select State "Karnataka"<br>2. Verify city dropdown options<br>3. Change State to "Maharashtra"<br>4. Verify city options updated | City dropdown populates with cities corresponding strictly to the selected state | Functional | Automated (`checkout.spec.ts`) |
| **TC-CHK-003** | Phone Number Validation (Negative) | Checkout modal open | 1. Enter phone "98765" (<10 digits)<br>2. Enter other fields<br>3. Click "Proceed to Payment" | Browser alert dialog triggers: "Please enter a valid 10-digit phone number." Focus returns to phone input | Negative | Automated (`checkout.spec.ts`) |
| **TC-CHK-004** | Pincode Validation (Negative) | Checkout modal open | 1. Enter pincode "6416" (<6 digits)<br>2. Fill valid phone & address<br>3. Click "Proceed to Payment" | Browser alert dialog triggers: "Please enter a valid 6-digit pincode." Focus returns to pincode input | Negative | Automated (`checkout.spec.ts`) |
| **TC-CON-001** | Contact Page Verification & Form | Contact page loaded | 1. Navigate to `/contact.html`<br>2. Check phone, address, hours<br>3. Fill inquiry form<br>4. Click "Send Message" | Store info cards and map display properly, form submit triggers confirmation alert | Functional | Automated (`contact.spec.ts`) |
| **TC-RSP-001** | Mobile Viewport Header & Menu Toggle | Viewport: iPhone 12 / Pixel 5 | 1. Load homepage on mobile viewport<br>2. Verify desktop menu hidden<br>3. Click hamburger icon<br>4. Verify mobile nav menu opens | Mobile menu toggles smoothly, mobile WhatsApp CTA button is visible | Responsive | Automated (`responsive.spec.ts`) |
| **TC-DAT-001** | Catalog Static Data Validation | Data test suite | 1. Fetch `data/products.json`<br>2. Validate schema attributes (`id`, `category`, `price`, `comboPrice`, `minimumOrder`, `sizes`) | All 10 products adhere strictly to required schema with non-empty attributes | Data Validation | Automated (`catalog-data-validation.spec.ts`) |
